import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fia-frontend-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
